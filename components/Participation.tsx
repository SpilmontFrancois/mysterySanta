import {View} from 'react-native';
import {TProfile} from '../types/profile';

type Props = {
  profile: TProfile;
};

const Participation = ({profile}: Props) => {
  return <View></View>;
};

export default Participation;
